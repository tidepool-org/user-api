'use strict';
var should = require('should');
describe('Tidepool user API', function(){
  it('should have user test', function(done) {
    var isTrue = true;
    should.exist(isTrue);
    done();
  });
});